




Cell.distance_between_pts
=========================


.. py:method:: single_cell_parser.cell.Cell.distance_between_pts(sec1, x1, sec2, x2)


   Computes the path length between two points.

   Points are specified by either their locations, or by point IDs and section IDs.

   Currently, this function uses the built-in NEURON method ``distance`` to compute the distances.
   Note that this approach may be inefficient for large numbers of synapses due to repeated computations.
   Additionally, the computed distances are approximate since NEURON calculates distances between the centers of segments.

   Future improvements could include implementing a look-up table for pair-wise distances to enhance efficiency.

   :param x1: Location or point ID of the first point.
   :type x1: float | int
   :param x2: Location or point ID of the second point.
   :type x2: float | int
   :param sec1: Section or section ID containing the first point.
   :type sec1: Section | int
   :param sec2: Section or section ID containing the second point.
   :type sec2: Section | int

   :returns: The computed path length between the two points.
   :rtype: float



.. container:: doc-feedback

   Documentation unclear, incomplete, broken or wrong? `Let us know <https://github.com/mpinb/in_silico_framework/issues/new?template=documentation.md&labels=docs>`_