

_map_complete_anatomical_realization
====================================


.. Signature -------------------------------

.. function:: single_cell_parser.network.NetworkMapper._map_complete_anatomical_realization(weights=None, full_network=False)


   Connect synapses to active presynaptic cells.

   This is step 4 in the creation of a saved network using :func:`create_saved_network2`.
   The synapses should already be created in :func:`_assign_anatomical_synapses` (step 1),
   the presynaptic cells have been created in :func:`_create_presyn_cells` (step 2),
   and their activity should have been generated in :func:`_activate_presyn_cells` (step 3).

   Assigns synapse weights using :func:`~_assign_synapse_weights` and connect synapses to presynaptic cells with defined activity.

   :param weights: pecific weights for each synapse type.
                   Default: None, in which case they are read from the :ref:`network_parameters_format`.
   :type weights: dict | None, optional
   :param full_network: Defines which cell IDS to use.
                        If True: (non-sequential) cell ids from the network embedding are used.
                        If False, single_cell network embedding from single_cell_input_mapper is used, in which cell ids are sequential.
   :type full_network: bool



.. container:: doc-feedback

   Documentation unclear, incomplete, broken or wrong? `Let us know <https://github.com/mpinb/in_silico_framework/issues/new?template=documentation.md&labels=docs>`_